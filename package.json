{
  "name": "ielts-foundation-notes",
  "version": "1.0.0",
  "private": true,
  "description": "Pre-IELTS foundation notes React app.",
  "scripts": {
    "build": "npm --prefix 1-code/next run build",
    "dev": "npm --prefix 1-code/next run dev",
    "start": "npm --prefix 1-code/next run dev",
    "preview": "npm --prefix 1-code/next run preview"
  }
}
