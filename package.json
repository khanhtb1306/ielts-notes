{
  "name": "ielts-foundation-notes",
  "version": "1.0.0",
  "private": true,
  "description": "Pre-IELTS foundation notes React app.",
  "scripts": {
    "build": "npm --prefix next run build",
    "dev": "npm --prefix next run dev",
    "start": "npm --prefix next run dev",
    "preview": "npm --prefix next run preview",
    "legacy:build": "node build.mjs",
    "legacy:start": "node build.mjs && npx http-server . -p 5173 -c-1",
    "legacy:serve": "npx http-server . -p 5173 -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
